package com.chess;

import com.ai.Action;
import com.ai.BreakTest;
import com.ai.MiniMax;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

public class CheckMateTest {

    @Test
    void checkMateIn2() {
        //todo
        //given
//        Position position = PositionGenerator.fromFEN("6k1/2r2p1p/5Pp1/6Q1/8/8/5PPP/6K1 w - - 0 1");
//        when
//        MiniMax miniMax = new MiniMax(new BreakTest());
//        Action action = miniMax.search(position);
//        then
//        Assertions.assertThat(((Move) action).getStartingSquare()).isEqualTo(Square.G5);
//        Assertions.assertThat(((Move) action).getEndingSquare()).isEqualTo(Square.H6);
//        given
//        Position newPosition = position.newState(action);
//        when
//        Action action2 = miniMax.search(newPosition);
//        then
//        Assertions.assertThat(((Move) action2).getStartingSquare()).isEqualTo(Square.H6);
//        Assertions.assertThat(((Move) action2).getEndingSquare()).isEqualTo(Square.G7);
    }
}
